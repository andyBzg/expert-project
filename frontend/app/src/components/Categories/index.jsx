import React from 'react';
import Container from '../UI/Container';

const Categories = () => {
    const category_list = [
        {id: 1, title: ''}
    ]

    return (
        <Container>
            
        </Container>
    );
};

export default Categories;